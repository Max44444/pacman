To play run:
python pacman.py -n <number of iterations> -x <number of logged steps(equals to number of iterations)> -p PacmanQAgent -l <layout> -q > <filename>
To display score from file run:
python ./plots/plot.py <filename>
